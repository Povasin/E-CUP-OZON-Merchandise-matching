# Локальные зависимости для разработки и v1 (прямое TF-IDF решение).
# Рантайм в докере использует baseline-образ; эти пакеты в нём уже присутствуют.
pandas
pyarrow
numpy
scikit-learn

# v2 (метод embed) — тяжёлые, ставить только для экспериментов с эмбеддингами:
# torch
# sentence-transformers
